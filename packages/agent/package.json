{
  "name": "@sudau/mcp-hub",
  "version": "0.2.0",
  "description": "A local stdio MCP agent that loads personalized remote config and connects to upstream MCP services.",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist/cli.js",
    "README.md",
    "LICENSE"
  ],
  "bin": {
    "mcp-hub": "dist/cli.js",
    "mcp-agent-platform": "dist/cli.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "agent",
    "codex",
    "claude-code",
    "opencode"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@mcp-agent-platform/runtime": "workspace:*",
    "@mcp-agent-platform/shared": "workspace:*"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "prepack": "pnpm run build",
    "lint": "echo \"(no lint configured)\""
  }
}
