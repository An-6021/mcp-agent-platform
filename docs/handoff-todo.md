# 继任待办

## 当前结论
- 前端已进入可演示、可内部试用状态，但还不是正式完工版。
- 本轮明确不做：admin 鉴权。
- 本轮明确不做：补测试。

## 优先继续
- 私有接入改成多令牌或客户端级令牌。
  现状：重新生成令牌会直接覆盖旧令牌，旧客户端会失效。
  目标：允许新增、复制、吊销单个令牌，不影响其他已接入客户端。

- 统一接入动作。
  现状：列表页公开服务复制的是命令，私有服务跳详情；详情页复制的是各厂商配置。
  目标：统一成一套心智，优先直接给可复制的厂商配置。

## 托管能力
- npm 托管未做。
  最小闭环：录入 npm 包、入口命令、环境变量、启动/停止、重启、日志、健康状态。

- 单文件托管未做。
  先不做重点，排在 npm 托管之后。

## 体验补完
- 能力说明展示继续打磨。
  当前已能看到 tools/resources/prompts，但信息密度和可读性还可以继续压缩。

- 列表页与详情页的产品文案继续收口。
  目标：更像配置中心，不像工程面板。

## 交接提醒
- 私有配置目前会把明文 token 写进复制出的客户端配置。
- 旧 token 只保存摘要，不能回显原文。
